import 'package:flutter/material.dart';

class TripDurationLayout extends StatelessWidget {
  const TripDurationLayout({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('TRIP DURATION'),
      ),
    );
  }
}
